# Suscripción, Referidos y Panel de Administración EFISCO

> Ver también: [README](../README.md) · [ARCHITECTURE](../Arquitectura%20y%20Sistema%20Core/ARCHITECTURE.md) · [SECURITY](../Arquitectura%20y%20Sistema%20Core/SECURITY.md) · [API](../Arquitectura%20y%20Sistema%20Core/API.md) · [BUSINESS_RULES](BUSINESS_RULES.md) · [OPERATIONS](OPERATIONS.md) · [INVENTORY](INVENTORY.md) · [FINANCE](FINANCE.md) · [FINANCIAL_ENGINE](FINANCIAL_ENGINE.md) · [GROWTH_ACQUISITION](../Estrategia%20Comercial%20y%20Ventas/GROWTH_ACQUISITION.md) · [PRICING_SALES](../Estrategia%20Comercial%20y%20Ventas/PRICING_SALES.md) · [MONITORING](../MONITORING.md) · [TESTING](../TESTING.md)

## Referidos

Sistema de referidos entre talleres con descuentos acumulados.

| Suscripciones referidas | Descuento aplicado |
|:---:|:---|
| 1 | 33% sobre cuota mensual |
| 2 | 66% sobre cuota mensual |
| 3+ | 100% (mes gratis) |
| Platino (>5) | 15% comisión directa (aplicado por EFISCO) |

- **"Quién te refirió" es de una sola vez y sin ciclos**: `linkReferral` rechaza el intento si el taller ya tiene un referidor vinculado, y recorre la cadena de referidos hacia arriba antes de guardar — un taller no puede vincularse con otro que, directa o indirectamente, ya fue referido por él mismo (rompería el árbol jerárquico de Admin → Red de Referidos, que necesita una raíz). No afecta el nivel propio: el nivel sube por cuántos OTROS talleres usan tu código, sin límite, sin importar si tú ya fuiste referido
- "Talleres que Referiste" (dashboard del dueño) lista los talleres que usaron tu propio código, con fecha de unión y ubicación — separado de "¿Alguien te recomendó?", que es la relación inversa (quién te trajo a ti)
- Banco/entidad para el pago de comisiones: selector con los 18 bancos más comunes de Colombia + "Otro" con texto libre
- **"Tu Estatus Actual" ahora es un botón funcional** (2026-07-25): antes era solo un badge decorativo — al tocarlo no pasaba nada. Ahora abre un modal comparando los 4 niveles (Base/Plata/Oro/Platino, mismos umbrales de la tabla de arriba) con el descuento y comisión de cada uno, resalta el nivel actual del taller, y muestra "Te faltan N referidos" para los niveles aún no alcanzados
- **Bug visual corregido 2026-07-25**: el texto "Ingresa el código de quien te invitó..." usaba `text-slate-600 dark:text-slate-300` dentro de una tarjeta con fondo oscuro en **ambos** temas (`bg-slate-900 dark:bg-black`) — la variante de modo claro asumía un fondo claro que esa tarjeta nunca tiene, quedando casi ilegible. Corregido a `text-slate-300` fijo (sin variante `dark:`), ya que el fondo siempre es oscuro ahí

---

## Panel de Administración EFISCO

Panel interno en `/admin` con autenticación completamente separada de los talleres — ver [SECURITY.md — Autenticación Admin](../Arquitectura%20y%20Sistema%20Core/SECURITY.md#autenticación-admin-panel-efisco).

### Dashboard

4 KPIs: talleres activos/totales, **órdenes del mes**, ingreso real de EFISCO en el mes, pagos de comisiones pendientes. El KPI de ingreso (`getStats.revenue_month`) suma `efisco_accounting_ledger` con `direction='income'` — mismo criterio que usa la pestaña [Contabilidad](#contabilidad-admincontabilidad) — no la venta bruta de los talleres a sus propios clientes (bug corregido: antes sumaba `cash_flow_ledger`/`INC_GROSS` de todos los talleres, un número sin relación con lo que el taller le paga a EFISCO — podía superar los $40M con solo un puñado de talleres activos). El cobro real de EFISCO es por **orden liquidada** (1 unidad de tarifa por orden en `settleOrder`, acumulado en `pending_service_fees`), sin importar cuántos servicios agrupe ni el valor de la orden. **2 tarifas según volumen (2026-08-05, reemplaza el valor fijo por taller `software_valuation_unit`, que quedó sin efecto en el cobro)**: progresivo dentro del mes calendario en curso, contando las órdenes 'completed' del taller con `start_time` desde el día 1 — las primeras 119 órdenes liquidadas en el mes pagan $535 COP (con IVA), desde la orden 120 en adelante pagan $416 COP (con IVA). El mismo taller puede cruzar de tarifa a mitad de mes; no es retroactivo (no se recalculan las órdenes ya cobradas ese mes). La factura Dataico de "suscripción taller" sigue sin desglosar IVA como línea de impuesto (`taxes: []` en `dataico.service.js:emitEfiscoInvoice`) — el monto acumulado ya es el valor final.

**"Órdenes del mes" cuenta órdenes, no servicios** (decisión de producto, 2026-07-30 — revierte a propósito el modelo de la rev. 44): `getStats.orders_month` (y las mismas cifras en `getWorkshops`/`getWorkshopDetail`, ver [Talleres](#talleres) abajo) cuentan ÓRDENES completadas del mes. Entre la rev. 44 y esta decisión, el cobro real de EFISCO fue por SERVICIO liquidado (`software_valuation_unit` × cantidad de filas en `work_order_services` de cada orden) — se volvió a tarifa por orden porque una orden con varios servicios debe seguir costando lo mismo que una de un solo servicio. El helper `countCompletedOrders` (`admin.controller.js`, antes `countCompletedServices`) ya no consulta `work_order_services`: cuenta directamente las órdenes completadas del mes en curso.

**"Mes en curso" (`getStats`/`getWorkshops`/`getWorkshopDetail`) calculado en hora de Bogotá** — bug corregido en rev. 47: `monthStart` se armaba con `new Date().getFullYear()`/`.getMonth()` (locales al proceso de Node, solo correctos si el servidor corre en UTC) o con `.toISOString().substring(0,7)` (UTC explícito) según el punto — ambos caminos podían adelantar el corte de mes hasta 5h respecto a la medianoche real de Bogotá, sub/sobre-contando órdenes e ingreso de EFISCO justo en la noche del cambio de mes. Los 3 unificados en `startOfMonthUtcIso()`/`currentMonthLocal()` (`backend/utils/localDate.js`), mismo criterio que ya usaba `getMonthlyBooks` (rev. 25/Bug #14, ver [Flujo de Caja — Libros Mensuales](FINANCE.md#libros-mensuales-archivados)).

### Talleres

Lista paginada y buscable de todos los talleres:
- **Búsqueda con debounce real** — bug corregido en rev. 42: el `setTimeout` de 400ms vivía dentro del `onChange` (`handleSearch`); el `return () => clearTimeout(t)` de un event handler nunca lo ejecuta nadie, solo funciona dentro de un `useEffect` — cada tecla disparaba un fetch nuevo sin cancelar los anteriores, con el riesgo real de que una respuesta vieja pisara resultados más nuevos. Movido a un `useEffect` con `clearTimeout` real en el cleanup
- Ver configuración completa (grid de campos, incluye "Tarifa/orden actual" — derivada en el cliente de `orders_month` contra el umbral de 120, ver arriba, ya no un valor fijo por taller —, "Órdenes mes" — órdenes liquidadas del mes en curso —, conteo/total de "Consultas de puntaje" — derivado de `pending_score_query_fees / 100`, sin contador dedicado —, y conteo/total de "Repuestos vendidos (mostrador)" — derivado de `pending_parts_sale_fees / 150`, ver [Venta Directa — tarifa por repuesto](#venta-directa--tarifa-por-repuesto-2026-08-05) abajo)
- Columna "Órd./Mes", columna "Consultas Score" (conteo + total en pesos) y columna **"Repuestos Vendidos"** (2026-08-05, misma mecánica) en la tabla principal — la columna "Tarifa/orden" de la tabla ya no lee `software_valuation_unit` (sin efecto en el cobro): se deriva del mismo `orders_month` de esa fila contra el umbral de 120
- **Contrato de Afiliación** (nueva sección en el detalle del taller, rev. 45): antes la prueba legal de firma (`workshop_contract_acceptances`) se guardaba pero nadie podía verla — ni el propio admin. Muestra estado firmado/sin firmar, fecha, versión del contrato, IP y el hash de firma (`GET /api/admin/workshops/:id` ahora hace `SELECT` a `workshop_contract_acceptances`, ver [API.md](../Arquitectura%20y%20Sistema%20Core/API.md#admin-apiadmin) y [SECURITY.md](../Arquitectura%20y%20Sistema%20Core/SECURITY.md#decisiones-técnicas-de-seguridad)); botón "Ver contrato" reutiliza `ContractViewerModal.jsx` (el mismo visor de solo lectura de `Config.jsx`) para leer el texto completo firmado con el mismo banner de constancia
  - **Descargar PDF** (rev. 46): junto a "Ver contrato", nuevo botón que descarga el PDF firmado del contrato del taller — `GET /api/admin/workshops/:id/contract-pdf` (`admin.controller.js#getContractPdf`), gateado por `requireAdmin`. Reutiliza el mismo servicio `contractPdfStorage.service.js` (`generateAndStoreContractPdf` / `getContractPdfSignedUrl`) que ya usaba el dueño desde `/config` (`GET /api/auth/contract-pdf`), incluyendo el self-heal: si la fila de auditoría no tiene `pdf_storage_path` (la generación falló al momento de firmar), lo regenera bajo demanda resolviendo el email del dueño vía `supabaseAdmin.auth.admin.getUserById`. Antes el admin solo podía leer el contrato en pantalla, sin forma de obtener el PDF para archivo/soporte
- Crear taller nuevo (crea usuario Supabase Auth + `workshop_config` en transacción)
- **Estado del taller — 3 vías (2026-07-29, pedido explícito)**, en este orden de prioridad:
  1. **"En proceso"** (ámbar): `admin_activated_at` todavía es `null` — el taller solicitó/se autorregistró pero EFISCO no le ha enviado el email de acceso, así que su dueño no ha podido poner una contraseña real todavía. Antes esto se veía igual que "Activo" (`is_active` nace en `true` por defecto), dando la impresión falsa de que el taller ya podía operar.
  2. **"Activo"** (verde): `admin_activated_at` con valor e `is_active !== false`.
  3. **"Suspendido"** (rojo): `admin_activated_at` con valor e `is_active === false`.
- **Suspender / Reactivar** — `PATCH /api/admin/workshops/:id/toggle` actualiza `is_active` en `workshop_config`
  - La suspensión bloquea el login (`auth.controller.js`) Y los requests en curso (`auth.middleware.js`) — SÍ bloquea el acceso real del taller
  - El taller suspendido ve una pantalla de "Cuenta suspendida" con canales de contacto
  - **Con confirmación** (mejora de UX, rev. 43): antes se ejecutaba con un solo clic, sin ningún paso intermedio, a diferencia de "Eliminar" que sí lo tenía — un clic accidental bloqueaba de inmediato el acceso de TODO el personal de un taller que paga por el servicio. Ahora pasa por `ElegantConfirmModal` (mensaje distinto según se vaya a suspender o activar)
  - **Pestaña "Suspendidos" (2026-07-29)** — `GET /api/admin/workshops?view=suspended` reusa `is_active` (ninguna columna nueva) para mostrar en su propia vista, separados de la lista normal, solo los talleres con `is_active === false`; el botón "Activar" de siempre (mismo `handleToggle`) los saca de esa vista al reactivarlos, sin necesitar un botón "Restaurar" aparte.
- **Eliminar (registro no activado)** — `DELETE /api/admin/workshops/:id`, botón visible solo para talleres con `admin_activated_at` vacío (autorregistros que un admin nunca activó). Borra en cascada todos los datos del taller y notifica por correo al dueño vía Resend (best-effort — si el correo falla, el borrado ya se considera exitoso). Un taller ya activado no muestra este botón — para ese caso sigue siendo "Suspender". **Exige la contraseña del propio admin de EFISCO como confirmación** (agregado 2026-07-29, verificada contra `efisco_admins`, nunca la contraseña del taller) — con un campo de contraseña dentro del mismo `ElegantConfirmModal` de siempre. *Historial de esta decisión*: se probó primero un rediseño a soft-delete ("Eliminar" solo ocultaba el taller del panel, `admin_hidden_at`, disponible para CUALQUIER taller) — se revirtió el mismo día por pedido explícito del usuario: los talleres sin activar son solicitudes de acceso, y alguien mandando muchas (spam/duplicados) llenaría la base de datos para siempre si solo se ocultaran en vez de borrarse. El gate por `admin_activated_at` y el borrado real en cascada volvieron tal cual estaban; lo único que quedó del intento de rediseño es la confirmación con contraseña de admin. Ver el razonamiento completo en [SECURITY.md](../Arquitectura%20y%20Sistema%20Core/SECURITY.md).
- **Marcar / desmarcar taller de prueba (2026-07-30, pedido explícito)** — `PATCH /api/admin/workshops/:id/toggle-test` (`admin.controller.js#toggleTestWorkshop`) actualiza `is_test_workshop` (columna nueva, `workshop_config`, migración `2026-07-30_workshop_is_test_flag.sql`) para diferenciar los talleres que el propio equipo crea para probar funcionalidades de los talleres reales. **Exige la contraseña del propio admin de EFISCO como confirmación**, mismo patrón que "Eliminar" (`ElegantConfirmModal` con input de password) — un clic accidental sobre un taller real distorsionaría silenciosamente los KPIs de plataforma. Badge "PRUEBA" (ámbar) junto al nombre del taller donde aplique. La lista de Talleres pasó de un booleano `showSuspended` a un query param `?view=active|suspended|test` con 3 vistas mutuamente excluyentes — "active" (default) excluye tanto suspendidos como talleres de prueba; "test" (nueva pestaña, mismo patrón visual que "Suspendidos") muestra solo `is_test_workshop = true`. **`getStats` excluye por completo los talleres de prueba de los 4 KPIs**: no cuentan en `total_workshops`/`active_workshops`, sus órdenes no suman a `orders_month`, y el ingreso de su propia suscripción (`efisco_accounting_ledger` con `reference_type='workshop_config'` y `reference_id` = su id) no suma a `revenue_month` — filas de ingreso sin taller asociado (otros SKUs) no se ven afectadas por este filtro.
- **Llave de acceso a cuenta del cliente** — ver flujo completo en [SECURITY.md](../Arquitectura%20y%20Sistema%20Core/SECURITY.md#llave-de-acceso-a-cuenta-del-cliente-soporte)
- **Factura propia de EFISCO al taller — EFISCO como proveedor real** (`documentation/Ampliación software.txt`, líneas 160 y 185): el admin sube el PDF y el monto/método de pago (`POST /api/admin/workshops/:id/software-invoice`) y el backend, en vez de guardar un registro aislado, provisiona (la primera vez, por taller) un proveedor real `EFISCO S.A.S.` en la tabla `providers` del propio taller — con los valores de la **plantilla editable** `efisco_provider_template` (ver Cobros abajo), y liquida la factura con **el mismo motor** que cualquier otra compra a proveedor (`financialEngine.liquidateSupplierPurchase`), aplicando ReteFuente/ReteICA/IVA/GMF **según la configuración fiscal que el contador le haya dado al taller** (Régimen Simple no retiene, dos grandes contribuyentes no se retienen entre sí, etc. — el gate ya existente se aplica también a EFISCO, tal como pide la documentación). La compra queda registrada en `supplier_purchases` y en `cash_flow_ledger` (igual que `registerPurchase`), y la fila de seguimiento en `efisco_software_invoices` se liga a esa compra vía `supplier_purchase_id`. El dueño ve la factura simple (monto/vencimiento/PDF) como "Gasto de Software" en `Config.jsx` (`GET /api/workshop/software-invoices`, signed URL de 1h) y ve el detalle completo de la liquidación (retenciones, IVA, historial) como cualquier otro proveedor en **Proveedores** (con una insignia "Proveedor EFISCO", sin botón de editar ni de eliminar — `updateProvider`/`deleteProvider` responden 403 si `is_system_provider` es true — y sin botón de "Registrar Compra", porque el taller no le compra nada a EFISCO: es EFISCO quien le factura al taller)

### Cobros (`/admin/cobros`)

Seguimiento de las facturas que EFISCO le emite a los talleres (`documentation/Ampliación software.txt`, línea 162: *"activar o desactivarle el software al cliente en caso de que pague o no"*):
- Card superior **"Plantilla proveedor EFISCO"**: NIT/nombre/ciudad/teléfono/régimen/IVA/declarante/tasa ICA/código PUC — única fuente de verdad de la identidad de EFISCO como proveedor (`GET/PUT /api/admin/efisco-provider-template`, tabla `efisco_provider_template`, fila única). Cada actualización se **sincroniza automáticamente** hacia la fila "EFISCO S.A.S." que cada taller ya tenga creada — el taller no puede editar su copia (`updateProvider` responde 403 sobre `is_system_provider`), evitando que un contador local desalinee los datos fiscales reales de EFISCO. `updateProviderTemplate` hace UPSERT (crea la fila si no existe, actualiza si ya existe) — antes solo sabía actualizar, así que si la tabla estaba recién migrada y vacía, quedaba permanentemente inutilizable ("corre la migración" para siempre aunque ya hubiera corrido); el frontend ya no bloquea la pantalla en ese caso, muestra el formulario vacío para llenar.
- Tabla de todas las facturas de todos los talleres (`GET /api/admin/software-invoices?status=`), enriquecida con nombre/NIT/estado del taller y link de descarga (signed URL). Marca "Vencida" en el frontend si `due_date` ya pasó y sigue `pendiente`.
- **Marcar como pagada** (`PATCH /api/admin/software-invoices/:id/mark-paid`): cambia `status` a `pagado` + `paid_at`, y **reactiva el taller** (`workshop_config.is_active = true`) en la misma operación — decisión explícita: el pago del software es lo que condiciona el acceso.

### Pagos

Gestión de solicitudes de pago de comisiones por referidos:
- Filtros: pendiente / en proceso / pagado
- KPIs por estado
- `PATCH /api/admin/payouts/:id/mark-paid`

### Referidos (árbol admin)

Árbol jerárquico recursivo de referidos con colapso/expansión:
- Colores por nivel: gris (0), azul (1-2), púrpura (Platino: 3+)
- KPIs: total nodos, referidores activos, Platinos, comisiones totales
- Datos bancarios del taller (banco, tipo de cuenta, número) visibles bajo cada nodo con al menos un referido, para que EFISCO pueda procesar el pago de la comisión sin saltar a otra pantalla — aviso ámbar si el taller aún no los registró

### Contabilidad (`/admin/contabilidad`)

Caso de uso 2 de Dataico: EFISCO como emisor de sus propios ingresos/costos (`dataico.service.js`: `buildSupportDocNoResidente`, `buildSupportDocBasico`, `emitSupportDocument`, `emitEfiscoInvoice`), ya implementado en código:
- **Facturación propia de EFISCO**: resolución/prefijo DIAN y consecutivo propios (`efisco_billing_config`, fila única — mismo patrón UPSERT que la plantilla de proveedor en Cobros, mismo bug corregido: antes quedaba inutilizable si la tabla estaba vacía). **Probado end-to-end en producción real (rev. 28)**: la tabla nunca había tenido una fila hasta esta revisión — se pobló con la resolución/prefijo de Factura Electrónica de Venta de la cuenta real de EFISCO y se emitió un documento real (`POST /admin/accounting/subscription-invoice`, `DIAN_ACEPTADO`, CUFE real). De paso se corrigieron 2 bugs reales que nunca se habían ejercitado contra la API real de Dataico: `emitEfiscoInvoice` leía `workshop.legal_address_line` para la dirección del cliente facturado, pero esa columna no existe (`workshop_config.address` es la real) — la dirección siempre salía "N/A"; y `emitSubscriptionInvoiceForWorkshop`/`recordWorkshopSideExpense` guardaban `dataico_document_id` leyendo `dataicoResult?.id || dataicoResult?.document_id`, campos que la respuesta real de Dataico nunca trae (trae `cufe`/`uuid`/`pdf_url`) — el CUFE real quedaba sin registrar en el ledger propio de EFISCO ni en el número de factura del espejo de gasto del taller.
- **Captura manual** de lo que ya se cobró/pagó por fuera del sistema (sin pasarela de cobro real todavía): Infraestructura No Residente (AWS/Vercel/etc., convierte a COP con la TRM del día — Documento Soporte a No Residente, SKU "costos infraestructura"), MP/WhatsApp/Dataico, Salarios/Honorarios (Documento Soporte Básico), Suscripción de Taller.
- **"Suscripción Taller" fusiona las dos contabilidades del mismo hecho económico en un solo paso** (`subscriptionBilling.service.js:emitSubscriptionInvoiceForWorkshop`): calcula el precio con descuento por referidos, emite la factura DIAN real de EFISCO al taller, registra el ingreso en el libro de EFISCO, **y además** dispara automáticamente el gasto espejo en la contabilidad del propio taller (mismo motor `financialEngine.liquidateSupplierPurchase` que usa "Talleres → Gasto de Software", mismo monto ya descontado, sin pedir PDF — la factura Dataico recién emitida es el respaldo legal). Checkbox opcional "Aplicar 4x1000 (GMF)" para cuando el pago real fue por transferencia bancaria (apagado por defecto). El botón manual de "Talleres → Gasto de Software" sigue existiendo intacto como vía alterna para casos puntuales (backdating, ajustes). El webhook real de Mercado Pago (`POST /api/billing/webhook/mercadopago`) ya usa esta misma función: el checkout (`POST /api/finance/subscription/checkout`) cobra por la pasarela el monto YA con descuento por referidos aplicado (mismo tier que calcula esta función), y codifica el monto sin descontar como `workshopId:baseAmount` en el `external_reference` de Mercado Pago para que el webhook sepa cuánto saldar de `pending_service_fees` al facturar — antes el checkout cobraba el total sin descuento y esta función facturaba después por menos, así que un taller con descuento pagaba de más por la pasarela frente a su propia factura DIAN (bug corregido en rev. 25).
- **Las consultas de score facturables se cobran en el mismo ciclo que la tarifa por orden** (bug corregido en rev. 44): `pending_score_query_fees` (ver [Recepción](OPERATIONS.md#recepción) — $100 COP por consulta con ≥30 días de historial, `verifyScoreOTP`) se acumulaba desde que existe la funcionalidad, pero ningún flujo de cobro lo tocaba ni lo reseteaba — ni el checkout de Mercado Pago del taller ni la factura manual del admin lo incluían; el único sitio que lo tocaba era un script interno de borrado de datos de prueba, así que solo crecía para siempre sin reflejarse nunca como cobrado. Decisión del usuario (vía AskUserQuestion): sumarlo a la misma factura de suscripción, manteniendo la diferencia visible para que el taller sepa qué se le está cobrando. Ahora `emitSubscriptionInvoiceForWorkshop` reparte el monto del ciclo primero contra `pending_service_fees`, luego contra `pending_score_query_fees` y por último contra `pending_parts_sale_fees` (saldando los tres), dejando el desglose ("tarifa por orden: $X + consultas de puntaje: $Y + venta directa de repuestos: $Z") en la descripción de la factura Dataico, el ledger propio de EFISCO y el concepto del libro mayor del taller cuando aplica; el checkout de Mercado Pago y la factura manual del admin ahora suman los tres campos por defecto, y `AdminAccounting.jsx`/`Config.jsx` (card "Tu suscripción" del propio taller) precargan el monto ya sumado con un hint del desglose completo.

#### Venta Directa — tarifa por repuesto (2026-08-05)

Tercer acumulado, mismo patrón exacto que las consultas de score: `pending_parts_sale_fees` (`workshop_config`, migración `2026-08-05_workshop_parts_sale_fee.sql`) se suma $150 COP por cada unidad vendida vía "Venta Directa" (mostrador, `inventory.controller.js:sellDirect` — no bloqueante, si falla la acumulación la venta ya quedó registrada igual). **A propósito no aplica a repuestos añadidos a una orden de servicio** (`addItemToWorkOrder`) — esos ya están cubiertos por la tarifa por orden liquidada (ver arriba). `PARTS_SALE_FEE_COP` está espejado en `AdminWorkshops.jsx` (columna "Repuestos Vendidos") y en `Tarifas.jsx`/`Config.jsx` del lado del taller — debe coincidir con `SCORE_QUERY_FEE_COP` (`clients.controller.js`) en el mismo patrón de constante duplicada front/back documentado en varios otros puntos de esta app.
- Comisiones de referidos pagadas disparan un Documento Soporte Básico (sku "pago referido") de forma no bloqueante — el pago ya quedó marcado como completado aunque Dataico falle.
- Ledger contable propio de EFISCO (`efisco_accounting_ledger`, sin `workshop_id`) con totales de ingreso/costo/neto por SKU.
- ~~El bloqueo para emitir esto en producción real no es de código: EFISCO necesita completar su propia habilitación de facturación electrónica ante la DIAN~~ — **resuelto en rev. 28**: EFISCO ya completó la habilitación DIAN (numeración asociada a Dataico como proveedor tecnológico) y Caso 2 quedó probado en real (ver arriba). El único requisito operativo que sigue siendo 100% manual es asociar la resolución/prefijo dentro del propio dashboard de Dataico (Ventas → Facturas → Numeraciones, botón "seleccionado") además de tramitarla ante la DIAN — tener la autorización de la DIAN no basta, Dataico necesita su propio paso de activación interno.

### Reportes Financieros (`/admin/reportes-financieros`)

Panel de carga para la página pública de Relación con Inversionistas (`frontend/src/pages/ReportesFinancieros.jsx`, `/reportes-financieros` — ver [BUSINESS_RULES.md](BUSINESS_RULES.md#rutas-del-frontend)), 2026-08-03:
- El admin sube un PDF (`period_type`: trimestral/anual, `period_label` ej. "1T 2026", `title`) desde un formulario simple — `POST /api/financial-reports` sube el archivo y crea la fila en `efisco_financial_reports` en un solo paso, sin modal ni paso intermedio.
- Bucket de Storage propio (`efisco-financial-reports`), **público** — mismo criterio que las fotos de herramientas (`tools.controller.js`): son documentos informativos para inversionistas, no confidenciales, y la página que los lista no tiene sesión — un bucket privado obligaría a firmar una URL por cada descarga. Se crea on-demand desde el controller si aún no existe.
- La tabla tiene RLS activado sin policies (deny-by-default) — solo el backend con `service_role` la toca; el listado público pasa siempre por `GET /api/financial-reports`, nunca por acceso directo de Supabase desde el navegador.
- Eliminar borra el archivo del bucket y la fila en el mismo paso (`DELETE /api/financial-reports/:id`) — sin confirmación en dos pasos más allá del modal estándar (`ElegantConfirmModal`).
- La página pública (`ReportesFinancieros.jsx`) separa lo listado en dos columnas (Anuales / Trimestrales) filtrando por `period_type` en el propio frontend, y no rompe si el backend no responde (se queda en el estado "Aún no hay reportes").

Las rutas API completas del panel admin están en [API.md — Admin](../Arquitectura%20y%20Sistema%20Core/API.md#admin-apiadmin) y [API.md — Financial Reports](../Arquitectura%20y%20Sistema%20Core/API.md#financial-reports-apifinancial-reports).
